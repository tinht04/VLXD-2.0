{
  "version": 2,
  "builds": [
    { "src": "backend/package.json", "use": "@vercel/next" },
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/backend/$1" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
